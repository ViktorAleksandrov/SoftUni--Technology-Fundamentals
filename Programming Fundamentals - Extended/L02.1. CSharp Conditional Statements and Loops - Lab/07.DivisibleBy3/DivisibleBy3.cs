using System;

class DivisibleBy3
{
    static void Main()
    {
        for (int currentNumber = 3; currentNumber <= 100; currentNumber += 3)
        {
            Console.WriteLine(currentNumber);
        }
    }
}